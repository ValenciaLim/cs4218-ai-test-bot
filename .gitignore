node_modules/
.env
k6-generated/
fixtures/

# Regenerated by `node index.js --agent=...` (Playwright + k6 MCP handoff JSON).
# If you use a custom output.mcpHandoffDir in agent.config.json, add that path too.
mcp-handoff/

# Playwright MCP client output (e.g. console-*.log) when using MCP from this repo.
.playwright-mcp/

.DS_Store
